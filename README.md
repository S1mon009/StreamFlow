# YouTube Downloader 📥

A versatile, open-source Python project offering both a Command-Line Interface (CLI) and a Graphical User Interface (GUI) for fetching and saving YouTube videos effortlessly.

---

## 🚀 Features

- **Dual Interfaces**: Choose between a lightweight CLI or a modern GUI (Flet-based).
- **Flexible Downloads**: Support for single videos, playlists.
- **Resolution Control**: Pick specific resolutions or let the tool choose the best available.
- **Resume Capability**: Automatically pick up where interrupted downloads left off.
- **Cross-Platform**: Works on Windows, macOS, Linux, Android, and iOS (via Flet).

---

## 🔧 Installation & Setup

1. **Clone the repository**
   ```bash
   git clone https://github.com/S1mon009/YouTube-downloader.git
   cd YouTube-downloader
   ```
2. Create & activate a virtual environment
   ```bash
   python -m venv venv
   source venv/bin/activate  # Linux / macOS
   venv\Scripts\activate.bat  @Windows
   ```
3. Install dependencies
   ```bash
   pip install -r requirements.txt
   ```
   
